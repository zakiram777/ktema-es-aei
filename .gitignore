# 이 사이트를 웹호스팅에 올릴 때는 필요 없는, 미리보기 전용 파일들.
# README.md 1장에도 적혀 있다 — 올려도 아무 일도 하지 않지만
# 깨끗한 저장소를 위해 추적하지 않는다.

# Claude Code 로컬 개발 설정 (프리뷰 서버 실행용, 개인 환경)
.claude/

# 각자 브라우저에만 남는 개인 설정을 내보낸 파일 — 실수로 올리면
# 그 설정이 사이트에 처음 들어오는 모든 사람에게 적용된다.
settings.json

# OS/에디터 잡동사니
.DS_Store
Thumbs.db
